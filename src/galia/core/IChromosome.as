package galia.core
{
	/**
	 * An intentionally empty interface.  A chromosome may be any object.
	 * This interface serves as a clarification/identification tool for objects being used as chromosomes
	 * for a given algorithm's setup.
	 */
	public interface IChromosome
	{
		
	}
}